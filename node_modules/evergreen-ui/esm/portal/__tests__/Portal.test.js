import React from 'react';
import { render, screen } from '@testing-library/react';
import Portal from '../src/Portal';
describe('Portal', function () {
  it('should append container to document body', function () {
    render( /*#__PURE__*/React.createElement("div", null, "children"));
    render( /*#__PURE__*/React.createElement(Portal, null, "children"));
    var elements = screen.getAllByText('children');
    expect(elements[1]).toHaveAttribute('evergreen-portal-container');
  });
  it('should render children', function () {
    render( /*#__PURE__*/React.createElement(Portal, null, /*#__PURE__*/React.createElement("span", {
      "data-testid": "children"
    }, "Hello world")));
    var children = screen.getByTestId('children');
    expect(children).toHaveTextContent('Hello world');
    expect(children.parentNode).toHaveAttribute('evergreen-portal-container');
  });
  it('should remove DOM element when unmounted', function () {
    var _render = render( /*#__PURE__*/React.createElement(Portal, null, "children")),
        unmount = _render.unmount;

    unmount();
    expect(screen.queryByText('children')).toBeNull();
    expect(document.querySelector('[evergreen-portal-container]')).toBeNull();
  });
});