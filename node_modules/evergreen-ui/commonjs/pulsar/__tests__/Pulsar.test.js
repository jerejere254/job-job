"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _extends2 = _interopRequireDefault(require("@babel/runtime/helpers/extends"));

var _react = _interopRequireDefault(require("react"));

var _react2 = require("@testing-library/react");

var _userEvent = _interopRequireDefault(require("@testing-library/user-event"));

var _utils = require("../../test/utils");

var _tooltip = require("../../tooltip");

var _Pulsar = require("../src/Pulsar");

var makePulsarFixture = function makePulsarFixture() {
  var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  return /*#__PURE__*/_react["default"].createElement(_Pulsar.Pulsar, (0, _extends2["default"])({
    "data-testid": "Pulsar"
  }, props));
};

makePulsarFixture.displayName = "makePulsarFixture";
describe('Pulsar', function () {
  it('should forward ref', function () {
    var ref = (0, _utils.mockRef)();
    (0, _react2.render)(makePulsarFixture({
      ref: ref
    }));
    expect(ref.current).toBeInstanceOf(HTMLDivElement);
  });
  it('should forward box props', function () {
    var backgroundColor = 'red';
    (0, _react2.render)(makePulsarFixture({
      backgroundColor: backgroundColor
    }));
    expect(_react2.screen.getByTestId('Pulsar')).toHaveStyle({
      backgroundColor: backgroundColor
    });
  });
  it('should render Tooltip when wrapped and hovered', function () {
    (0, _react2.render)( /*#__PURE__*/_react["default"].createElement(_tooltip.Tooltip, {
      content: "Hello world"
    }, makePulsarFixture()));

    _userEvent["default"].hover(_react2.screen.getByTestId('Pulsar'));

    expect(_react2.screen.getByText('Hello world')).toBeInTheDocument();
  });
});